{
  "name": "BeaverHours",
  "version": "0.0.1",
  "description": "",
  "author": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install:bot": "cd bot && npm install",
    "install:tabs": "cd tabs && npm install",
    "installAll": "npm run install:bot & npm run install:tabs"
  },
  "devDependencies": {
    "@microsoft/teamsfx-cli": "0.*",
    "prettier": "2.5.1"
  },
  "license": "MIT",
  "dependencies": {}
}
